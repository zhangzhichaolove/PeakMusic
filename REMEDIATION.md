# PeakMusic 整改目标

状态：开发中。对应 2026-09-05 确认的双项目整改清单，不代表全部完成。

## 执行顺序与验收方式

1. 核心正确性：写回归测试，修复后运行单测、Lint、Debug/Release 构建。
2. 核心体验：播放页、在线搜索、按需权限、启动、本地媒体刷新、主题及小屏布局。
3. 工程和功能：媒体身份、队列传输、数据库、发布流程，再补队列/歌单/歌词功能。
4. 真机验收独立记录，不用 JVM 测试代替耳机、息屏、音频焦点、系统回收等实测。

## P0：已实现，真机回归待执行

- [x] M01：缓冲期间暂停取消自动播放意图；耳机断开、睡眠定时和用户暂停后的焦点恢复遵守同一规则。
- [x] M01：播放错误可通过服务重试当前音源，不依赖首页回调；文件权限失效转为可展示错误。
- [x] M02：实际播放队列独立持久化；混合来源跨曲后恢复正确；重新扫描本地库不覆盖当前队列。
- [x] M02：兼容旧队列偏好设置；完整队列仅在变更时序列化，进度仍节流保存。
- [ ] 真机：弱网准备期间暂停/拔耳机/定时到期后不出声；混合队列在进程重建后保持曲目、顺序和位置。

## P1：核心体验

- [x] M03 播放页提供播放/暂停、上下首、错误重试、收藏、模式、队列入口；元数据/歌词可滚动、底部进度/播放控制固定，API 36 横屏 200% 字体实测通过。全设备主题验收仍归 M08。
- [x] M04 在线搜索调用现有 `music/getMusicList?search=` 接口；在线/本地分区，350ms 防抖、立即取消旧请求、空查询/空结果、失败重试和页面销毁取消；范围在重建后保留。
- [x] M05 首页不再串联申请音频/通知/悬浮窗；本地页显式授权，拒绝后有设置修复入口；通知独立设置、悬浮控制显式开关且默认关闭。在线播放不依赖本地权限。
- [x] M06 移除固定 2 秒启动等待，保留旧启动组件直接进入首页。
- [x] M07 手动刷新、前台 MediaStore 变化监听/防抖、返回前台更新；区分未授权/扫描中/空库/失败，权限撤回使在途扫描失效。
- [ ] M08 已改善首页/本地页背景、搜索与曲目文字对比度、播放栏 48dp 按钮和底部手势避让；全页面深浅色、横屏/大字体巡检继续开发。
- [x] M09 连接测试真实列表端点，校验 2xx、业务成功与 records/mp3 结构；有整体超时/响应体上限，关闭对话框取消请求，忽略已修改地址的旧结果。

## P1：工程

- [ ] C01 歌词请求/偏移与队列持久化已分别独立为 LyricsLoader/LyricOffsetStore、PlaybackStorage；继续收敛服务内的播放意图/音频焦点状态协调，不做全项目重写。
- [x] C02 API 基址命名空间 + 稳定媒体 ID，播放 URL 独立保存；迟到响应/相对地址固定到发起请求的来源。收藏、歌单、歌词偏移与服务队列使用身份键；同 URL 不同来源不串曲，签名更新不丢收藏/校准。旧在线数据保持 legacy 原键、本地 MediaStore Uri 键继续兼容，缺 ID 的 URL 回退不承诺跨 URL 变化合并。
- [x] C03 Room v1→v2 为歌单成员增加级联外键及音源索引，保留有效收藏/历史/成员并清理旧孤儿关系；元数据使用 Upsert，避免 REPLACE 触发级联删除。歌单/历史写入事务、升级及注入失败后的回滚测试通过，v1/v2/v3 schema 已导出；v2→v3 保留原键/外键成员，只增加来源与播放地址字段。
- [x] C04 首页在线/本地、搜索、收藏/歌单和下一首统一先在私有存储保存队列，再仅通过 Intent 传 queue_id；移除全量列表 AIDL 与 Intent 入口。队列 JSON 编解码、状态读写在串行后台线程，进度写入复用已存队列，元数据随队列保存并在选中时落库。10,000 条真实服务 IPC、播放、持久化与单页大小验证通过。
- [ ] C05 CI 构建/单测工作流已加入；本地页面/生命周期/大队列原生回归及 minified Release 黑盒脚本已加入。已新增 API 33/36 原生矩阵及完整本地 CI 入口；远端工作流与云模拟器任务仍待验收。
- [ ] C06 已修复 Release 反射 DTO/队列字段保留；诊断改为 Debug 进程内显式开启、有界脱敏、分享确认、旧原文清理和独立 FileProvider 导出，Release 禁止采集。环境签名/版本注入、APK 证书/版本/R8 映射核验与归档已实现；按数据源 HTTP 信任及真实发布凭据/远端流程验收仍待完成。

## P2：功能

- [x] M10 可编辑队列：曲目菜单加入下一首，队列页上移/下移/指定位置/下一首/移除/确认清空；定位当前歌曲、分页与页面重建。修改使用版本检查拒绝过期操作；排序保留当前曲目与进度，删除当前曲目保留原播放/暂停意图，清空取消准备并停止播放，重复音源的当前条目可恢复。
- [x] M11 歌单重命名、选中歌单重建恢复、曲库多选/批量收藏/复制到歌单/移除成员；本地全部歌曲/歌手/专辑/文件夹浏览、分类内播放和返回、重建后分类保留及媒体新增/删除刷新。元数据不足的分类使用明确回退，不推断真实身份。
- [x] M12 歌词偏移按当前歌曲 source 保存（±10 秒，旧全局值仅迁移给首个明确歌曲）；区分加载/无歌词/失败并提供重试，取消旧歌曲请求、防止迟到结果串歌，有整体超时和体积限制。同源同 ID 的 URL 改变后继续使用同一校准键；旧 URL 校准保留给来源未知的旧记录，不猜测迁移到新 API。

## 首批验证

- 新增 MusicService 的 Robolectric 回归：准备中暂停/继续、耳机断开、睡眠定时、用户暂停取消焦点恢复、权限失效、错误重试、混合队列重建。
- 已运行 `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease`：26 项测试通过，Debug/Release 构建通过。
- 真机/弱网/息屏测试未执行；未覆盖安装到用户手机。
- Release 仍为未签名产物，正式发布配置继续跟踪。

## 第二批验证（2026-09-05）

- `:app:testDebugUnitTest :app:lintDebug :app:assembleDebug :app:assembleRelease :app:assembleDebugAndroidTest`：45 项 JVM 测试通过，Debug/Release 和测试 APK 构建通过。Lint 仅原有 `activity_api_logs.xml` 的 NestedWeights 提示。
- 专用 API 36 模拟器：真实接口搜索、旧查询取消/新查询显示、503 重试、本地分区与页面重建；音频/通知拒绝状态下首页和本地提示页可用。2 项原生用例通过。
- 可复跑 `scripts/verify-experience.sh`；日志与截图位于 `app/build/verification/experience/`。该脚本仅用于可清空应用数据的测试模拟器。
- 已修复验证中发现的本地提示背景对比度、列表副标题对比度及播放栏被手势区遮挡。
- 设备权限弹窗的不同 OEM 行为、真实耳机/焦点/息屏等专项仍未完成。开发目标继续保持进行中。

## 第三批验证（2026-09-05）

- 歌词/收藏/播放模式和歌单重命名：原生静音 WAV、真实歌词 HTTP 503→重试、404 无歌词、跨歌曲偏移、页面重建及 Room 收藏落库通过。修复从搜索/歌单播放时服务未保存封面/歌词元数据的问题。
- Room 增加 7 项仓储回归，覆盖 Upsert 保留成员、清历史保留收藏/成员、失效歌单、重命名、v1→v2 升级及添加成员/清历史/删除歌单中途失败的事务回滚。
- 可编辑队列新增服务回归：重排保留音频/进度、删除当前歌曲时保留暂停/播放意图、版本过期拒绝、清空准备中的音频并重建、下一首及重复 URL 当前条目恢复、10,000 条分页大小约束；兼容无 current_source 的旧索引队列，并修复顺序队列末尾手动下一首导致实际播放与状态不一致。
- 最近全量验证：71 项 JVM 测试；Debug/Release/AndroidTest 构建通过，Lint 仅原有 activity_api_logs.xml 的 NestedWeights 警告。API 36 专用模拟器 6 项原生用例覆盖搜索、按需权限、歌词/收藏/模式、200% 字体横屏、歌单重命名、队列排序/移除/清空/重建。
- 证据：`app/build/verification/experience/`，包含构建、instrumentation 日志与截图；`scripts/verify-experience.sh` 可复跑。字体用例临时改模拟器 font_scale 后恢复，未对用户手机安装或清数据。
- 仍未验收全设备/OEM、真实耳机/息屏/音频焦点、全部页面深浅色；本批不改写这些待办为已完成。

## 第四批验证（2026-09-05）

- PlaybackStorage 以私有原子文件暂存完整队列，只传 UUID；写入成功才发播放 Intent。快速连续点击只接收最后一个请求，暂停/清空可以取消正在加载或尚未送达的请求；过期/消费过的 ID 不覆盖后来编辑的队列。
- 服务启动异步恢复状态，旧索引/来源队列继续兼容；暂停、睡眠定时、清空在读取期间仍生效。尚未交接的持久队列可在进程重建后继续接收，成功保存播放状态后才删除暂存；失败不替换当前队列，提供存储错误反馈。
- 新增 8 项存储测试和 7 项服务竞态/恢复测试。已移除三份冗余队列存储及每首播放前整队列批量写 Room；完整封面/歌词等元数据随队列恢复，当前曲目按需写入 Room。
- API 36 专用模拟器：10,000 条队列通过真实 startForegroundService IPC；Intent 仅 356 字节。一次本机样本的提交主线程 CPU 约 3.9ms、队列和音频就绪约 1.0 秒，提交调用的 StrictMode 主线程磁盘读写告警为 0。此为单台模拟器样本，不作为所有设备的性能保证。
- Release 实测发现既有 HttpResult/MusicListModel 在 R8 后被裁剪，造成首页 ClassCastException；已为反射 DTO 保留类/字段。`scripts/verify-queue-release.sh` 以本地 HTTP 样本验证 Release 接口列表，并将 Debug 暂存的 10,000 条队列交给真正 minified Release 恢复，选中静音 WAV 的 MediaSession 为 PLAYING，实际识别时长 60,000ms。
- Release 脚本只用默认调试证书签署 unsigned APK 的副本，正式发布产物与签名配置不变；结束时停止测试音频、关闭本机样本服务/端口转发，并恢复 Debug 应用/测试 APK。仅接受可丢弃模拟器，不操作用户手机。
- 最新全量验证：86 项 JVM 测试、7 项常规原生用例通过，Debug/Release/AndroidTest 构建通过；Lint 仅原有 NestedWeights 警告。额外 minified Release 平台黑盒验证通过。完整证据见 `app/build/verification/experience/` 与 `app/build/verification/queue-release/`。远端 CI、正式签名和真实硬件专项仍未验收。

视频端已完成共享播放设置、稳定来源命名空间与明确分集地址的客户端契约，验证记录在 ChaoVideo 的 REMEDIATION.md；不计作本项目的音乐源身份验收。音乐稳定数据源/媒体身份已完成本批客户端整改；下一批优先处理歌单批量/本地分类浏览及发布隐私流程，再补齐全页面主题与设备回归。

## 第五批验证（2026-09-05）：音乐来源与媒体身份

- 以规范化 API 基址（含协议/端口/路径）命名空间 + 源内媒体 ID 为身份，音频地址独立保存；数值/字符串 ID 及 ID 0 可用，缺 ID 才使用 URL 回退。ServiceFactory 捕获来源，迟到响应及相对封面/歌词/音频不受后续设置切换影响。
- Room v3 只增加来源/媒体 ID/播放地址列，在线旧记录保留 legacy 和原键，本地记录按已存 local 标记保留原 Uri/路径键。v1/v2→v3 单测验证原收藏、播放次数、本地元数据和外键歌单关系；实际模拟器的 6 条旧曲目逐列完整保留，foreign_key_check 通过，证据见 `app/build/verification/sources/native-migration.txt`。
- 保持历史列名 source 作为库键，播放/下载/分享使用 getPlaybackUrl；歌单仍用 Upsert，更新 URL 不触发 FK 级联删除。当前曲目回调只传一条完整元数据，首页/播放页不再从当前 API 列表按 URL 猜测身份；歌词偏移使用稳定键。队列暂存/恢复保留身份元数据与 current_key，仍兼容旧索引/URL 队列。
- 新增 12 项 JVM 测试，覆盖来源隔离、相对 URL/迟到响应、伪造 JSON 来源排除、数值/字符串 ID、Parcelable、Room 迁移/成员保持、歌词偏移、队列序列化与同 URL 不同源的恢复/回调。最新 98 项 JVM 测试全部通过，Debug/Release/AndroidTest 构建通过；Lint 仅原有 activity_api_logs.xml 的 NestedWeights 警告。
- 新增 1 项原生来源回归：真实 Retrofit A 迟到于切换 B，A/B 相同 ID 和相同音频 URL 仍有两条收藏及两项歌单成员；实际播放切换到各自歌词与 +1.0s/-0.5s 偏移。A 签名 URL 更新后保留收藏/成员/偏移，服务重建保持 A 身份、更新后 URL、12 秒位置及暂停意图。已检查来源列表与恢复播放页截图。
- 原 7 项搜索/权限/歌词/大字体/歌单/队列/10,000 条 IPC 原生回归全部复跑通过，合计 8 项常规原生用例。此次 10,000 条样本 Intent 仍为 356 字节，提交 StrictMode 磁盘告警 0；单台模拟器提交 CPU 约 3.36ms、队列/音频就绪约 1.05 秒，不作全设备保证。
- 实际混淆 Release 再次返回 RELEASE_QUEUE_OK：从 Debug 暂存的 10,000 条混合队列恢复一条具 API 身份键的在线曲目，通过独立 HTTP URL 解码 60 秒 WAV；相对歌词真实展示，恢复该身份键的 +1.5s 校准，MediaSession 为 PLAYING。测试只签署副本；结束恢复 Debug/标准测试入口、停止服务/映射并清理测试 API 地址，正式签名仍待发布方配置。
- 脚本 `scripts/verify-sources.sh emulator-5580` 不清旧数据以支持迁移检查；常规及 Release 脚本会清空可丢弃模拟器中本应用数据。全部 adb 操作限定 emulator-5580，未操作用户手机。来源日志/截图位于 `app/build/verification/sources/`；常规/Release 证据路径同第四批。
- 剩余主题/导航、歌单批量、本地分类、在线字幕、发布隐私、远端 CI 与真实硬件验收继续推进。缺 ID URL 变化、API 基址变更或旧记录未知来源不作自动身份合并；无服务端续签端点时不声称旧签名会自动刷新。
- 收尾检查发现原生测试 finally 中的 API 设置 apply() 可能尚未落盘就结束进程，遗留已关闭的 loopback 来源。双项目来源测试均改为同步落盘，并在复跑脚本增加 API 偏好快照/EXIT 恢复；这只影响测试隔离，不修改用户正常设置流程。已清理本次可丢弃模拟器上的已确认测试地址。

## 第六批验证（2026-09-05）：曲库多选与批量操作

- 收藏/历史/歌单曲目增加显式多选、全选/取消全选、数量提示、收藏/取消收藏、复制到歌单及移除当前歌单成员。点行/复选框只切换选择，不触发播放；非选择状态保留长按菜单。取消收藏/移除前确认，不删除任何音乐文件；回到歌单列表不遗留曲目多选入口。
- 批量写入只携带稳定库键，后台事务读取最新数据、预校验全部曲目与目标歌单并去重；保留已更新音频 URL、收藏、历史及其他成员关系。注入中途更新/插入/删除失败验证整批回滚，无部分提交；本批不修改数据库版本。
- ViewModel 保留配置重建中的已选键、忙状态及一次性结果。处理期间锁住重复操作/选择/返回，重建不丢完成回调；失败后解锁并保留仍有效的选择供重试。进程死亡后退出临时选择，不向 Bundle 填入无上限键数组。
- 选择变化只通知实际改变行的 checkbox payload，模式/忙状态变化通知范围更新，不触发整个列表失效或重复封面加载。原生截图检查后同时改用主题空状态文字颜色，避免批量移空后提示过浅。
- 新增 6 项 JVM 回归（4 项事务/失败回滚、1 项选择状态、1 项列表 payload），累计 104 项通过；Debug/Release/AndroidTest 构建通过，Lint 只有原有 activity_api_logs.xml 的 NestedWeights 警告。
- 新增 2 项 API 36 原生回归，真实点击双源同 URL 曲目/复选框、全选、旋转重建、收藏/取消收藏、复制与确认移除；断言历史/其他成员/文件保留、UI 旧 URL 不覆盖数据库新签名。另在 Room 事务阻塞期间重建页面，触发原生 SQLite 中途失败，验证未部分收藏、选择保留/解锁且随后重试成功。批量用例多次复跑通过，证据位于 `app/build/verification/batch/`。
- 原 7 项常规体验与 1 项来源回归复跑通过，累计 10 项原生用例；常规网络测试的 API 恢复也改为同步落盘，不遗留已停止的样本地址。无新 adb reverse 映射，未操作用户手机。
- 实际混淆 Release 返回 RELEASE_QUEUE_OK 和 RELEASE_LIBRARY_BATCH_OK：除了 10,000 条队列/真实音频/歌词，还从平台独立 runner 打开生产歌单页并创建混淆 ViewModel，点击批量收藏、复制及移除。平台 SQLite 核对 2 条收藏、目标 2 条成员保留、原歌单成员清零，本地文件仍存在。只测试签名 APK 副本，退出恢复 Debug/标准 runner；证据位于 `app/build/verification/queue-release/`。
- 双项目总目标保持进行中：M11 本地分类浏览、全页面主题/导航、视频在线字幕、发布隐私/签名注入、远端 CI 和真实设备专项尚未完成。视频本批未改生产代码，最近验证仍见其第五批记录。

## 第七批验证（2026-09-05）：本地媒体分类浏览

- 本地页增加全部歌曲/歌手/专辑/文件夹切换、分类数量及返回入口；组内点击只提交当前已显示列表，不再用分类位置索引首页整份本地曲库。服务回调统一更新当前播放信息，扫描、切换分类和删除媒体不会覆盖或误恢复已暂停的播放队列。
- 后台构建 MediaStore 快照索引；歌手未知值统一归类，专辑优先按存储卷与 ID、缺 ID 时按名称/歌手回退。Android 10+ 读取存储卷及相对目录，不依赖原始 DATA 路径；旧版本使用已知父目录，目录未知则明确显示未知分类，不遍历未授权文件系统。同名末级目录/不同卷不合并。
- 分类与组键保存在小型状态 Bundle，重建重新读取扫描结果。修复首页持有的 Fragment 数组在 FragmentStateAdapter 恢复后指向未展示实例的问题，扫描与 API 重载回调改投实际恢复的页面。系统 Back 先关闭抽屉/返回分类，再保留原进入后台行为。
- 媒体变化后保留当前分类；分类被删空显示说明和返回按钮。权限失效清除可见数据并使旧索引任务失效。新增 SongModel 存储卷/相对目录字段及 Parcelable 保留，不修改 Room 版本或既有收藏/歌单键。
- 截图巡检发现有专辑 ID 但没有内嵌封面时 Glide 失败会清空封面区域；为共用音乐封面加载器增加加载中/失败默认封面，原生分类场景检查封面仍可见，不新增网络请求。
- 新增 7 项 JVM 回归：歌手规范化/Uri 去重、专辑 ID/卷/合辑与无 ID 回退、同名目录隔离、Parcelable、10,000 条分类完整划分，以及 Android 10 无 DATA 路径时扫描相对目录。累计 111 项 JVM 通过；Debug/Release/AndroidTest 构建通过，Lint 仍只有原有 activity_api_logs.xml 的 NestedWeights 提示。已移除新代码中不兼容 API 21 的集合默认方法。
- 新增 1 项 API 36 原生回归：将带真实 ID3 标签的静音 MP3 写入自身 MediaStore 行，经真实扫描浏览三类，实际解码 MP3、识别至少 59 秒的媒体时长并进入 PLAYING；选中分类队列恰为 2 首。旋转后直接新增/删除 MediaStore 媒体，显示页更新且暂停队列保持；同名末级文件夹分离，删除所选目录最后一首后有空状态/返回入口。不是用测试列表注入代替真实扫描。
- 原 7 项常规、1 项来源、2 项批量原生用例复跑，加上本地分类合计 11 项。新脚本 `scripts/verify-local-library.sh emulator-5580` 生成带标签样本、恢复测试 API/音频权限并删除自身媒体，保留已有曲库；日志/截图位于 `app/build/verification/local/`。已检查歌手组、组内播放及完整目录区分截图。
- 真正混淆 Release 继续通过 10,000 条队列和批量操作，并新增 RELEASE_LOCAL_LIBRARY_OK：平台独立 runner 写入/浏览真实 MediaStore 专辑、目录及歌手，从分类播放本地 MP3，通过 MediaSession 时长/状态与队列页的 2 首计数验收，随后清空测试队列再删除媒体。证据见 `app/build/verification/queue-release/`。
- Release 测试曾因工具保留 MEDIA_CONTENT_CONTROL shell 身份，使 API 36 音频 AppOps 归属改变而被焦点加固拒绝。已将 shell 权限严格限于枚举会话，浏览/播放恢复应用自身身份后通过；没有降低生产音频焦点保护。测试首次清理还发现重复删除已不存在的 MediaStore 行会报权限异常，已记录自身删除状态实现幂等清理，并清掉该次遗留的两条已确认样本。
- M11 客户端功能验收完成；总目标继续推进全页面主题/导航、视频在线字幕、状态协调拆分、发布隐私/签名注入、CI 和真实设备专项，不以本批替代这些剩余验收。视频本批未修改生产代码。

## 第八批验证（2026-09-05）：诊断隐私与发布版验证

- API 日志从默认输出原文改为 Debug 显式开启、仅本进程有效，Release 禁止采集；请求头/正文、URL 路径/参数和异常消息不写入诊断或 logcat。最多旁读 1 MiB + 1 字节，原始 ResponseBody 保持完整且不由诊断文件托管，清空/淘汰日志及磁盘写入失败不破坏业务响应。
- 严格 JSON 流式脱敏覆盖常见凭据、签名、个人信息字段及 URL、Bearer/Basic、JWT、嵌入 JSON/常见本地路径；非 JSON、畸形、深度/节点数或体积超限保存省略说明。明确告知“有限规则脱敏，不保证任意自定义字段匿名”；复制正文/分享前确认，分享 chooser 仅接收二次脱敏的独立快照。
- cache/diagnostics-v1 与 diagnostic-exports 各限 20 个文件/20 MiB、单条/单导出 2 MiB、保留 24 小时；启动/下次访问清理，读取/导出拒绝已过期选择，不依赖后台定时器。后台删除旧 api-response-logs 原文，FileProvider 仅暴露导出目录。关闭/清空使旧采集票据失效，迟到响应不重新生成日志。
- 文件操作/格式化移至页面后台线程；补齐深浅色诊断动作对比度、系统栏避让。修复确认复制时后台返回触发列表刷新、使 content 变空而崩溃的问题，改用确认前的脱敏快照。Release 空状态明确禁用采集，不再提示点击禁用的开关。
- 新增 12 项 JVM 回归（4 脱敏、8 存储/响应完整/限制/迁移/在途失效/过期读取），累计 **123 项通过，零失败/错误/跳过**；Debug/Release/AndroidTest 构建通过，**Lint: No issues found**，原日志页 NestedWeights 布局也已消除。
- 新增 2 项 API 36 原生回归，真实 HTTP 请求、默认关闭/手动开启、原始字节、超限摘要、真实在途清空、预览/复制/分享及页面重建通过。复制确认中进入后台/返回仍正确；深浅色按钮至少 4.5:1 对比度。拦截系统 chooser 并通过 FileProvider 读取快照验证脱敏，未实际发送到其他应用/服务器；结束恢复剪贴板、停用采集和清空诊断。
- 7 项常规体验、1 项来源、2 项批量、1 项本地分类全部复跑通过，加上新诊断共 **13 项原生用例**。诊断复跑脚本 scripts/verify-diagnostics.sh；构建/原生日志和深浅色截图位于 app/build/verification/diagnostics/，已逐张检查。
- 平台独立 runner 在真正混淆 Release 中新增 RELEASE_DIAGNOSTIC_PRIVACY_OK：采集开关 disabled/unchecked、正确的 Release 空状态、旧原文清理、旧 FileProvider root 拒绝读取。清理后重新创建自己的 canary 再读取旧 URI，只接受无映射异常，不以“文件不存在”误报 root 验证成功；随后删除 canary。
- 全部安装/命令限定专用 emulator-5580，未操作连接的用户手机；已恢复 Debug 和 AndroidJUnitRunner、font_scale=1.0，无遗留 adb reverse 或 loopback 测试 API；音乐 Release 脚本按设计清空可丢弃应用数据后恢复默认来源，视频保留测试前偏好。生产签名产物仍未配置；测试签名只用于独立 APK 副本。
- 总目标继续进行中：按源 HTTP 信任、签名/版本/映射发布流程、全页面主题/导航、视频在线字幕、播放/投屏状态协调拆分、远端及模拟器 CI、真实硬件/OEM/电视专项不计作本批完成。

## 第九批验证（2026-09-05）：签名/版本/映射归档与 CI 入口

- 双项目接入环境版本与签名配置；普通构建继续 versionCode 1 / versionName 1.0、Release unsigned。显式版本必须成对且合法，显式签名必须同时提供版本与全部四项签名输入；缺项、不可读文件或错误密码失败，不悄悄输出 unsigned 冒充发布包。Debug 签名身份保持原调试证书。
- 新增 package-release.py：依次 JVM 测试、Debug/Release Lint、真实混淆构建，验 apksigner 成功退出/唯一期望身份、APK 内部包名/版本与非 debuggable；对 R8 mapping 正文校验 pg_map_hash，再与 DEX 的 pg-map-id 对应，拒绝错版或截断映射。不把 APK 名字、已打印证书或 mapping 文件存在当作验收。
- APK/mapping/版本与 Git 状态清单/SHA256SUMS 作为一个不可覆盖 ZIP 归档；不包含私钥、密码或密钥库路径，不自动上传或发布。脏工作区如实标记，不声称可重建或代替正式 tag 审核；商店版本单调性仍由发布方核对。Python 单元回归每项目 8 项通过，覆盖密钥/版本缺项、错误证书、debuggable、陈旧/拆分/跨包输出、路径逃逸、错映射/正文篡改、非零验签退出、哈希与归档不可覆盖。
- verify-release-config.py 用临时 2 天有效 RSA/PKCS12 证书，真实构建版本 42 / 0.0.0-verification。默认 unsigned、版本缺项/越界、部分签名配置、正确签名/归档、错误证书/映射、错误密码全部按预期验收，返回 RELEASE_CONFIG_OK；证书结束删除，测试 ZIP 留在 verification/release-config/TEST-ONLY-release.zip，不混入正式归档目录。脚本不继承调用方的实际发布签名输入，最后恢复默认 Debug/Release/AndroidTest 产物，未将此临时证书安装到设备。
- 实测发现 build-tools 37 将签名输出从 Signer #1 改为 V3.0 Signer；已支持两种格式并仍要求成功验签、单一身份及匹配指纹，没有因格式变化跳过证书校验。验证记录与默认恢复结果见 app/build/verification/release-config/。
- 新增 verify-native-ci.sh：合成媒体、全部常规原生、真正混淆 Release 顺序执行；只接收 API 33+ 可丢弃 emulator-*，每条 adb 明确序列号，退出停止本应用。API 36 专用 emulator-5580 已返回 NATIVE_CI_OK，13 项常规原生及本项目全部 Release 标记通过。
- Android workflow 加入打包策略/临时证书回归及 API 33/36 x86_64 原生矩阵，保留日志/截图；新增仅手动、先依赖完整验证 workflow 成功、再在 release environment 下运行的签名归档 workflow（验证阶段不传发布秘密），密钥放 runner 私有临时文件并清理，不使用配置缓存或发布 Gradle 缓存。双项目 actionlint 1.7.7 检查通过；**尚未推送或执行远端 Actions，不将本地 API 36 当作 API 33/云端 x86_64 验收**。
- 当前 **123 项 JVM + 8 项 Python 策略测试 + 13 项常规原生**通过，Debug/Release/AndroidTest 构建及 Debug/Release Lint 均通过（No issues found），额外临时证书签名/归档和真正混淆 Release 黑盒通过。正式证书/Secrets/环境审核人、长期映射存储和远端 CI 执行由发布方配置，详见 docs/RELEASE.md。
- 总目标保持进行中：按源 HTTP 信任、主题/导航、视频在线字幕、播放/投屏状态职责拆分、远端及真实硬件/OEM/电视专项仍有明确未完成项；本批没有提交、推送或使用正式私钥。
